import React from 'react';
  
  const Load-config = () =>  {
	return (
	  <div>
	  </div>
	);
  }
  
  export default Load-config;
  import { loadConfig } from '../lib/load-config'
export default loadConfig
