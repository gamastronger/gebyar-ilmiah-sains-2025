import React from 'react';
  
  const Is-windows = () =>  {
	return (
	  <div>
	  </div>
	);
  }
  
  export default Is-windows;
  module.exports = () =>
  process.platform === 'win32' || /^(msys|cygwin)$/.test(process.env.OSTYPE)
